---
import type { RichTextBlock } from '../../lib/content';

interface Props {
  block: RichTextBlock;
}

const { block } = Astro.props;
---

<div data-rich-text class="bp-rich-text-widget" set:html={block.html} />
